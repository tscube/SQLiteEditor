<Window x:Class="Sample1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Sample1"
        Height="450"
        Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- メニュー -->
        <Menu Grid.Row="0" Background="White">
            <MenuItem Header="終了(_X)" Click="Shutdown_Click"/>
            <MenuItem Header="SQL実行(_E)" Click="Execute_Click"/>
        </Menu>

        <!-- SQL入力 -->
        <TextBox Grid.Row="1" Text="{Binding Path=SqlStmt, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="MS Gothic"/>

        <!-- スプリッター -->
        <GridSplitter Grid.Row="2" Background="White"
                      Height="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                      ResizeDirection="Rows"/>

        <!-- 結果表示 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding DataList}"
                  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
